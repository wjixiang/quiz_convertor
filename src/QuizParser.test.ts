import { expect, describe, it } from 'vitest';

describe("Parse quiz text into structurized quiz data", () => {
    it("split full text into smell chunk: reduce reqirement of model agent ability", async()=>{
        
    })
})